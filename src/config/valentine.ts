const BASE_URL = import.meta.env.BASE_URL;

export const KATOO_LOGO_URL = `${BASE_URL}katoo_studio_dev_logo.png`;
// Replace this with an external URL if needed.
export const VALENTINE_VIDEO_URL = `${BASE_URL}VDK.mp4`;
